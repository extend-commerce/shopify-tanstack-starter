/**
 * Library version string (RR parity: `SHOPIFY_REACT_ROUTER_LIBRARY_VERSION`).
 * Kept in sync with this package's `package.json` `version`.
 */
export const SHOPIFY_APP_TANSTACK_START_VERSION = '0.0.0';
